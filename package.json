{
  "name": "dockernode",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "docker build -t dino96112/node-app .",
    "delete": "docker rm --force backend",
    "container": "docker container run -d --name backend -p 49160:5000 -e NAME=coba -e HOST=mysqlserver dino96112/node-app",
    "deploy": "npm run delete && npm run container "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mysql": "^2.18.1"
  }
}
